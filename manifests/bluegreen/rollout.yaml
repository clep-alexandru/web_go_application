apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: go-web-server-bluegreen
  namespace: bluegreen
spec:
  replicas: 2
  strategy:
    blueGreen:
      activeService: go-web-server-active
      previewService: go-web-server-preview
      autoPromotionEnabled: false
      autoPromotionSeconds: 30
  selector:
    matchLabels:
      app: go-web-server
  template:
    metadata:
      labels:
        app: go-web-server
    spec:
      containers:
        - name: go-web-server
          image: ghcr.io/clep-alexandru/web_go_application:7733b4d742f93cba2e1f41d4fbe4f82255c6c3df
          envFrom:
            - configMapRef:
                name: go-web-server-config
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: ghcr-secret
