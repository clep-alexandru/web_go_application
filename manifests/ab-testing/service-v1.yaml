apiVersion: v1
kind: Service
metadata:
  name: go-web-server-v1
  namespace: ab-deployment
spec:
  selector:
    app: go-web-server
    version: v1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP